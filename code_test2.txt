program my_program;

var i, j : int;
var b : bool;

def void uno (int a) {
	var i : int;
	b = false;
	i = a * 2;
	if (i < a + 4) {
		uno(a + 1);
	}
	print(i);
}

def int dos(int b) {
	b = b * i + j;
	if (b > 0 and b < 10) {
		return (b * 2);
	}

	i = j - 6 * 3;
	return b;
}

main {
	i = 2;
	j = i * 2 - 1;
	uno(j);
	print(i + dos(i + j));
}
