program patito;

var i, j : int;

def void uno (int a, int b) {
	var i : int;
	i = a * 2;
	if (i < a + 4) {
		uno(a + 1, 3);
	}
	print(i);
}

def int dos(int b) {
	b = b * i + j;
	return (b * 2);
}

main {
	i = 2;
	j = i * 2 - 1;
	uno(j, i);
	print(i + dos(i + j));
}
