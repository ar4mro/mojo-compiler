program patito;

var i, j : int;
var z : float;
var b : bool;

def void uno (int a) {
	var i : int;
	i = a * 2;
	if (i < a + 4) {
		uno(a + 1);
	}
	print(i);
}

def int dos(int b) {
	b = b * i + j;
	return (b * 2);
}

main {
	i = 2;
	j = i * 2 - 1;
	uno(j);
	print(i + dos(i + j));
}
